# SeatCard · 更新日志

## V0.26 · 2026-05-15

### 新功能
- **四主题系统**：新增「少女」（粉色日系）与「夜樱」（粉色暗夜）主题，与原「春日」「喜夜」合计4种风格循环切换（右上角按钮 / Ctrl+Shift+T）
- **服务器保存偏好弹框**：新用户首次进入主界面时弹出说明框，明确告知默认不上传服务器，用户自主选择是否开启自动保存；附免责声明提醒做好 CSV 备份
- **PDF 导出主题适配**：4种主题均有对应 PDF 配色（粉系/暗系），导出内容与当前界面风格一致

### 优化
- **桌名工具主题跟随**：「无/桌/席」后缀按钮、桌名模板 Tab、桌号快填按钮在切换主题后颜色即时更新，不再固定显示春日绿色
- **PDF 导出按钮图标**：将 `📄` emoji 替换为纯 SVG 文档图标，彻底消除喜夜/夜樱深色主题下 emoji 底色发黑的问题

---

## V0.25 · 2026-05

### 新功能
- **管理后台全面重构**：全新暗金主题 UI，支持全局自动保存配置、三种密码管理（管理/仪表盘/全局访问密码）
- **自动保存管理器**：`AutoSaveManager` 统一调度，支持定时、空闲、大操作三种触发方式；场次级三态开关（继承全局 / 强制开 / 强制关）
- **仪表盘密码 + setAS 接口**：dashboard.php 支持独立密码；save.php 新增 `?action=setAS` 接口供前端异步写入场次覆盖设置

---

## V0.24 · 2026-04-10

### 新功能
- **导入名单**：CSV 导入旁新增「📋 导入名单」按钮，支持纯文本格式（每行一人，逗号后为配偶姓名），自动识别并跳过标题行
- **触控平板适配**：iPad 等宽屏触控设备自动启用纵向分割布局（`touch-tablet` class），竖屏/横屏均适配
- **画布双击取消落座（触控）**：在画布座位胶囊上连续双击即可取消落座，与 PC 双击逻辑等效
- **mc-tag 鼠标拖拽后备**：紧凑列表的名牌标签新增 `draggable` 支持，触屏和鼠标均可拖动

### 优化
- **Shift+D 调试增强**：切换布局模式、宾客列表模式时均在控制台输出 `[LAYOUT]` / `[MODE]` 日志
- **projectTitle 单行锁定**：标题过长时自动截断省略，不再撑高 header
- **警告徽章隐藏区间**：1000–1200px 宽度范围下隐藏「⚠ 服务器存储不加密」提示，避免 header 拥挤
- **布局还原修复**：从移动/平板布局切换回 PC 横向布局时，自动清除内联 `flex` 残留，正确还原两栏比例
- **双击取消（触控名单）**：mc-tag 添加 `touch-action:manipulation`，修复 iOS 双击缩放干扰导致双击取消失效的问题

### 内容调整
- **modal 精简**：`firstRunModal` 移除服务器警告；`helpModal` 移除 `data/` 目录、`auth.json`、服务器部署等技术细节
- **admin.php 新增部署说明**：将技术信息（PHP 需求、目录权限、文件结构、安全提示）汇总到管理后台底部卡片

---

## V0.23 · 2026-04（历史）

- 触控拖拽 V4 系统（移动激活阈值，无长按计时器）
- 双指捏合缩放 / 平移画布（iOS Safari）
- 自定义分类（最多 6 方）
- 桌位排序拖拽（PDF 输出顺序）
- 暗夜模式 / 春日模式切换
- 多选桌子框选 + 整组拖动
- Ctrl+Z 撤销（40步）
- 查看码（7位只读链接）
- CSV 导入 / 导出 / 样板
